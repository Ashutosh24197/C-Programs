#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
  int a=0,n,m,l=0,i=1;
   prontff("Enter order om matrix : " );
   scanf("%d*%d",&n,&m);
   int martix[n][m];
   for(int j=a;j<n;j++)
    {
    matrix[a][j]=i;
    i++;
   }
   a++;
   for(int j=a;j<m;j++)
   {
       matrix[j][n]=i;
       i++;
   }
   n--;
   for(int j=)

}
